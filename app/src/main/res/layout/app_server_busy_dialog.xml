<?xml version="1.0" encoding="utf-8"?>
<!-- Dialog: Server Busy
     IDs: ivBusyIcon, tvBusyTitle, tvBusyMessage, btnRetry, btnGoBack -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/_16sdp"
        android:background="@drawable/app_face_dialog_rounded_slight"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingStart="@dimen/_24sdp"
        android:paddingTop="@dimen/_30sdp"
        android:paddingEnd="@dimen/_24sdp"
        android:paddingBottom="@dimen/_22sdp">

        <!-- Icon with glow -->
        <FrameLayout
            android:layout_width="@dimen/_80sdp"
            android:layout_height="@dimen/_80sdp">

            <View
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/app_logo_glow_update" />

            <ImageView
                android:id="@+id/ivBusyIcon"
                android:layout_width="@dimen/_52sdp"
                android:layout_height="@dimen/_52sdp"
                android:layout_gravity="center"
                android:contentDescription="@string/app_dialog_icon_text"
                android:scaleType="fitCenter"
                android:src="@drawable/app_transparent_close_choose" />
        </FrameLayout>

        <!-- Title -->
        <TextView
            android:id="@+id/tvBusyTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/_14sdp"
            android:fontFamily="@font/app_poppins_bold"
            android:gravity="center"
            android:includeFontPadding="false"
            android:text="@string/app_server_high_demand_text"
            android:textColor="#1A1A2E"
            android:textSize="@dimen/_18sdp" />

        <!-- Gradient accent bar -->
        <View
            android:layout_width="@dimen/_28sdp"
            android:layout_height="@dimen/_3sdp"
            android:layout_marginTop="@dimen/_8sdp"
            android:background="@drawable/app_face_badge_orange_gradient_bg" />

        <!-- Message -->
        <TextView
            android:id="@+id/tvBusyMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/_10sdp"
            android:layout_marginHorizontal="@dimen/_8sdp"
            android:fontFamily="@font/app_poppins_regular"
            android:gravity="center"
            android:includeFontPadding="false"
            android:lineSpacingMultiplier="1.4"
            android:text="@string/app_face_server_high_demand_desc_text"
            android:textColor="@color/app_secondary_text"
            android:textSize="@dimen/_12sdp" />

        <!-- Try Again button -->
        <TextView
            android:id="@+id/btnRetry"
            android:layout_width="match_parent"
            android:layout_height="@dimen/_46sdp"
            android:layout_marginTop="@dimen/_20sdp"
            android:background="@drawable/app_face_dialog_button_gradient_horizontal"
            android:fontFamily="@font/app_poppins_semibold"
            android:gravity="center"
            android:includeFontPadding="false"
            android:text="@string/app_face_server_busy_retry_button_text"
            android:textColor="#FFFFFF"
            android:textSize="@dimen/_14sdp" />

        <!-- Go Back (secondary) -->
        <TextView
            android:id="@+id/btnGoBack"
            android:layout_width="match_parent"
            android:layout_height="@dimen/_38sdp"
            android:layout_marginTop="@dimen/_4sdp"
            android:fontFamily="@font/app_poppins_medium"
            android:gravity="center"
            android:includeFontPadding="false"
            android:text="@string/app_face_data_busy_back_button_text"
            android:textColor="@color/app_tertiary_text"
            android:textSize="@dimen/_12sdp" />

    </LinearLayout>
</FrameLayout>
